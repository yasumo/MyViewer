<Page
    x:Class="MyViewer.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyViewer"

    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Height="600" Width="600"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <TextBox Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10,10,70,555" />
            <TextBox Text="{Binding LogText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="10,50" />
            <Button Content="インデックス作成" Command="{Binding CreateIndex}" HorizontalAlignment="Left" Margin="10,555,0,0" VerticalAlignment="Top" Width="119" Height="35"/>
            <Button Content="検索" Command="{Binding Search}" HorizontalAlignment="Left" Margin="535,10,0,0" VerticalAlignment="Top" Width="55" Height="35"/>
            <Button Content="スライドショー" Command="{Binding SlideShow}" HorizontalAlignment="Left" Margin="471,555,0,0" VerticalAlignment="Top" Width="119" Height="35"/>
        </Grid>
    </Grid>
</Page>
